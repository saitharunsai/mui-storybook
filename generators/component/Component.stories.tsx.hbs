import { ComponentMeta, ComponentStory } from '@storybook/react'

import {{ properCase name }} from './{{ properCase name }}'

 export default {
  title: 'MUI Default/{{ properCase folder }}/{{ properCase name }}',
  component: {{ properCase name }},
  argTypes: {
    color: {
      description: 'color for {{ properCase name }}',
      options: colorList,
      control: {
        type: 'radio',
        labels: labelList,
      },
    },
  },
} as ComponentMeta<TODO>

const Template: ComponentStory<TODO> = (props) => <{{ properCase name }} {...props} >Hello</{{ properCase name }}>

export const Default = Template.bind({})
Default.args = {}
