import { ComponentMeta, ComponentStory } from '@storybook/react'

import {{ properCase name }} from './{{ properCase name }}'

import { colorList, paletteNameList } from '../../../../designToken'

 export default {
  title: 'MUI Default/{{ properCase folder }}/{{ properCase name }}',
  component: {{ properCase name }},
  argTypes: {
    color: {
      description: 'color for {{ properCase name }}',
      options: colorList,
      control: {
        type: 'radio',
        labels: paletteNameList,
      },
    },
  },
} as ComponentMeta<TODO>

const Template: ComponentStory<TODO> = (props) => <{{ properCase name }} {...props}/>

export const Default = Template.bind({})
Default.args = {}
