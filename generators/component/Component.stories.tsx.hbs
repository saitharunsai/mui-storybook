import { ComponentMeta, ComponentStory } from '@storybook/react'

import {{ properCase name }} from '@mui/material/{{ properCase name }}'

import { paletteNameList } from '../../../../designToken'

 export default {
  title: 'MUI Default/{{ properCase folder }}/{{ properCase name }}',
  component: {{ properCase name }},
  argTypes: {
    bgcolor: {
      description: 'bgcolor',
      options: paletteNameList,
      control: {
        type: 'radio',
        labels: paletteNameList,
      },
    },
  },
} as ComponentMeta<TODO>

const DefaultTemplate: ComponentStory<TODO> = ({ children = 'Hello World', bgcolor = 'primary.main'}) => <{{ properCase name }} sx={ {bgcolor: bgcolor} } children={children} />

export const Default = DefaultTemplate.bind({})
Default.args = {}
